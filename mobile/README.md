# Customer Mobile App

Flutter customer app planned.

The previous React mobile prototype is retained only as a non-production reference. The customer-facing web experience lives in `frontend/` and the future mobile application will be implemented separately with Flutter.
